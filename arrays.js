var chocolateBars = ["snickers", "hundred grand", "kitkat", "skittles"];

function addElementToBeginningOfArray(chocolateBars, element){
  
  chocolateBars.unshift(chocolateBars, element);
  return chocolateBars;
}
